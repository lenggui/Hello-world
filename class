class Student(object):
    name = 'sudent'#给类绑定一个属性


s = Student()#创建实例s
print(s.name)#打印name属性，因为实例并没name属性，所以会继续查找class的name属性
print(Student.name)#打印类的name属性

s.name = 'guizi'#给实例绑定name属性
print(s.name)#打印实例属性，由于实例属性的优先级比类属性高，因此，他会屏蔽掉类的name属性
print(Student.name)#但是类属型并没有消失，用Student.name仍然可以访问

del s.name#删除实例的类属性
print(s.name)#再次调用s.name，由于实例的name属性并没有找到，类的name属性就显示出来了
